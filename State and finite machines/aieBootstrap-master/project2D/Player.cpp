#include"Player.h"
#include<stdlib.h>
#include<time.h>

void Player::update(float deltaTime){
	GameObject::update(deltaTime);

	
}


//m_position.x = (rand() % 1080) + 200;
//m_position.y = (rand() % 520) + 200;